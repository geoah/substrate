// Code generated by cmd/kindsgen from kinds/core.substrate.reamde.dev. DO NOT EDIT.
// `mise run kinds:gen` rewrites this file; an edit here is lost.

package corekinds

// Authority is core.substrate.reamde.dev/authority as its declaration admits
// it.
//
// One authority whose kinds this repository holds, named by its DNS-style
// label. The row says who may write under it and where its declarations came
// from, and carries the quarantine mark set when a stored closure no longer
// admits.
type Authority struct {
	// Name is the authority's DNS name.
	Name *string

	// Version is the declared maturity statement, v1alpha1 unless said
	// otherwise.
	Version *string

	// Actors is the actors declared under this authority.
	Actors []string

	// Source is builtin for seeded authorities, installed for bundle ones.
	Source *AuthoritySource

	// Quarantined is set at repository-open when the authority's stored
	// closure fails admission; cleared by re-applying a valid closure, or by
	// an open that admits the stored one.
	Quarantined *bool

	// QuarantineReason is the admission error that quarantined the authority.
	QuarantineReason *string
}

// AuthoritySource is a declared enum: the admissible set, in declaration
// order.
//
// builtin for seeded authorities, installed for bundle ones
type AuthoritySource string

const (
	AuthoritySourceBuiltin   AuthoritySource = "builtin"
	AuthoritySourceInstalled AuthoritySource = "installed"
)

// AuthoritySourceValues are the declared values in declaration order, which is
// render order.
var AuthoritySourceValues = []string{"builtin", "installed"}

// Valid reports whether v is one of the declared values.
func (v AuthoritySource) Valid() bool { return Declared(AuthoritySourceValues, string(v)) }

// AuthorityKeys is the admitted key set: every property
// core.substrate.reamde.dev/authority declares, sorted. A key outside it is
// refused by Decode, which is what replaces a hand-kept list of admitted keys
// in Go.
var AuthorityKeys = []string{"actors", "name", "quarantineReason", "quarantined", "source", "version"}

// DecodeAuthority decodes an authored properties map into Authority, refusing
// anything the declaration does not admit: an undeclared key, a value of the
// wrong type, an enum or state outside its set, a number outside its range, a
// required property left absent. The value is nil unless the problems are
// empty, so a half-decoded record can never reach a caller.
//
// Structural only. Whether the referent of a reference exists, whether a
// transition is legal from where the record stands, and whether this writer
// may write a managed property are the engine's questions, not this package's.
func DecodeAuthority(props map[string]any) (*Authority, []Problem) {
	d := &decoder{}
	out, _ := decodeAuthority(d, "", props)
	if len(d.problems) > 0 {
		return nil, d.problems
	}
	return &out, nil
}

// decodeAuthoritySource decodes a declared AuthoritySource value.
func decodeAuthoritySource(d *decoder, path string, v any) (AuthoritySource, bool) {
	s, ok := d.text(path, v, AuthoritySourceValues, nil)
	if !ok {
		return "", false
	}
	return AuthoritySource(s), true
}

// decodeAuthority decodes one Authority value at path.
func decodeAuthority(d *decoder, path string, v any) (Authority, bool) {
	props, mapped := d.mapping(path, v)
	if !mapped {
		return Authority{}, false
	}
	var out Authority
	for _, key := range sortedKeys(props) {
		// A null is this dialect's delete marker, never a value: it decodes as
		// absence, and Properties never writes one.
		if props[key] == nil {
			continue
		}
		switch key {
		case "name":
			p := at(path, "name")
			if e, ok := d.text(p, props[key], nil, nil); ok {
				out.Name = &e
			}
		case "version":
			p := at(path, "version")
			if e, ok := d.text(p, props[key], nil, nil); ok {
				out.Version = &e
			}
		case "actors":
			p := at(path, "actors")
			if items, listed := d.list(p, props[key]); listed {
				list := make([]string, 0, len(items))
				for i, item := range items {
					if e, ok := d.text(index(p, i), item, nil, nil); ok {
						list = append(list, e)
					}
				}
				out.Actors = list
			}
		case "source":
			p := at(path, "source")
			if e, ok := decodeAuthoritySource(d, p, props[key]); ok {
				out.Source = &e
			}
		case "quarantined":
			p := at(path, "quarantined")
			if e, ok := d.flag(p, props[key]); ok {
				out.Quarantined = &e
			}
		case "quarantineReason":
			p := at(path, "quarantineReason")
			if e, ok := d.text(p, props[key], nil, nil); ok {
				out.QuarantineReason = &e
			}
		default:
			d.unknown(at(path, key), "core.substrate.reamde.dev/authority")
		}
	}
	return out, true
}

// Properties is Authority as the properties map holds it, and
// DecodeAuthority's exact inverse: a nil pointer, a nil slice and a nil map
// each omit their key, so absence survives the round trip.
func (v *Authority) Properties() map[string]any {
	out := map[string]any{}
	if v.Name != nil {
		out["name"] = *v.Name
	}
	if v.Version != nil {
		out["version"] = *v.Version
	}
	if v.Actors != nil {
		items := make([]any, 0, len(v.Actors))
		for i := range v.Actors {
			items = append(items, v.Actors[i])
		}
		out["actors"] = items
	}
	if v.Source != nil {
		out["source"] = string(*v.Source)
	}
	if v.Quarantined != nil {
		out["quarantined"] = *v.Quarantined
	}
	if v.QuarantineReason != nil {
		out["quarantineReason"] = *v.QuarantineReason
	}
	return out
}

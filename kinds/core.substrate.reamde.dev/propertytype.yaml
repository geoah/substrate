# one custom property type: a refinement of a base type, e.g. asin
kind: core.substrate.reamde.dev/kind
metadata:
  id: core.substrate.reamde.dev/propertytype
data:
  authority: core.substrate.reamde.dev
  description: >-
    One custom property type: a built-in datatype refined by a pattern, a
    range or a value set (`isbn`, `asin`). Authority-local — only the authority
    that declares it may use it — so a shape is stated once instead of repeated
    on every property that wants it.
  # v1alpha2: the display template dropped the authority. A declaration's
  # identity is `<authority>/<name>` and the dotted composite it used to render
  # names nothing — the bump is what carries the new template into repositories
  # that already stored the old one.
  # v1alpha3: the declaration gained a `description` — the bump is what
  # carries it into repositories that already stored the v1alpha2 one.
  # v1alpha4: TYPED. `definition` and `name` are gone; the refinement — the
  # pattern, the range and the value set — is properties.
  version: v1alpha4
  names:
    singular: propertytype
    plural: propertytypes
  # id = "<authority>/<name>". A refinement is authority-local — a property type is
  # only usable inside the authority that declares it.
  displayTemplate: "{localName}"
  properties:
    version:
      type: string
      managed: true
      description: this declaration's version
    source:
      type: enum
      values:
        - builtin
        - installed
      managed: true
      description: builtin for seeded refinements, installed for bundle ones
    authority:
      type: string
      required: true
      description: the one authority it is usable in
    description:
      type: string
      description: what the refinement is for
    base:
      type: string
      required: true
      description: the built-in datatype it refines
    pattern:
      type: string
      description: the regular expression a value matches
    min:
      type: float
      description: the lowest admitted number
    max:
      type: float
      description: the highest admitted number
    values:
      type: object
      repeated: true
      description: the admitted values, in render order
      fields:
        value:
          type: string
          required: true
          description: the stored value
        label:
          type: string
          description: the human label a client renders beside it

# one declared trait: reusable typed properties a record type binds
kind: core.substrate.reamde.dev/kind
metadata:
  id: core.substrate.reamde.dev/trait
data:
  authority: core.substrate.reamde.dev
  description: >-
    One declared trait: reusable typed properties with shared semantics
    that a kind binds — `temporal`, `bundleconfig`, `accountconfig`.
    Traits resolve across authorities, and host behavior keys on the trait
    a kind binds rather than on what the kind is called.
  # v1alpha2: the display template dropped the authority. A declaration's
  # identity is `<authority>/<name>` and the dotted composite it used to render
  # names nothing — the bump is what carries the new template into repositories
  # that already stored the old one.
  # v1alpha3: the declaration gained a `description` — the bump is what
  # carries it into repositories that already stored the v1alpha2 one.
  version: v1alpha3
  names:
    singular: trait
    plural: traits
  # id = "<authority>/<name>". Traits resolve across authorities, so a vocabulary
  # type can bind core's `temporal` without redeclaring it.
  displayTemplate: "{name}"
  properties:
    # Every declaration carries its own version: the
    # declaring authority's, unless the declaration overrides it. It is what the
    # boot-time upgrade diffs a repository's shipped vocabulary against, so
    # it is written on every projected declaration row.
    version:
      type: string
      description: this declaration's version
    name:
      type: string
      description: the trait's local name
    authority:
      type: string
      description: the authority that declares it
    definition:
      type: json
      description: the parsed manifest data
    # No sourceYAML (record 61): schema rows store parsed facts only.

/** `react/jsx-runtime` inside the guest: what the transform's automatic
 * runtime imports, re-exported from the console's own React. Named, for the
 * reason `react.ts` gives. */
export { Fragment, jsx, jsxs } from "react/jsx-runtime"
